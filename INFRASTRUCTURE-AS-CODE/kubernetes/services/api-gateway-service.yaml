apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: ai-agent-system
  labels:
    app: api-gateway
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: api-gateway